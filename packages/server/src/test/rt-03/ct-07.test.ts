
import initializeDatabase from "../arrange/initialize-database";

describe("CT-07", () => {
    describe("GIVEN an existing user to update", () => {
        it("SHOULD update the user successfully", async () => {
            // Arrange
            await initializeDatabase();

            //ver como faz no front

            // Act
            

            // Assert
            
        });
    });
});
